#include<iostream>
using namespace std;
int main()
{
	string str,str2;
	cout<<"Enter the string : "<<endl;
	getline(cin,str);
	
	int start,length;
	cout<<"Input the position to start extraction :";
	cin>>start;
	
	cout<<"Input the length of substring :";
	cin>>length;
	
	int j=0;
	cout<<"The substring retrieve from the string is :";
	for(int i=start-1;i<length+start && i<str.length();i++)
	{
//		str2[j++] = str[i];
		cout<<str[i];
	}
	
//	cout<<str2;
	
	return 0;
}
